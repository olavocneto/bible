{
  "name": "bible",
  "private": true,
  "version": "0.0.1",
  "description": "Bible App",
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watchAll",
    "generate": "stencil generate",
    "deploy": "npm run build && cd www && surge"
  },
  "dependencies": {
    "@stencil/core": "^1.11.2",
    "@stencil/router": "^1.0.1",
    "bootstrap": "^4.6.0"
  },
  "license": "MIT",
  "devDependencies": {
    "@stencil/sass": "^1.1.1",
    "@types/jest": "25.1.4",
    "@types/puppeteer": "2.0.1",
    "jest": "26.6.3",
    "jest-cli": "26.6.3",
    "puppeteer": "2.1.1"
  }
}
